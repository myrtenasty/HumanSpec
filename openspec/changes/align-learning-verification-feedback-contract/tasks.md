## 1. Stack and Contract Foundations

- [ ] 1.1 Rebase on the completed project-context runtime change and verify its public plan/apply/reconcile envelopes remain unchanged by this evidence work.
- [ ] 1.2 Define canonical version-1 learning-feedback types, status values, marker constants, typed record shapes, validation issues, and duplicate identity rules.
- [ ] 1.3 Add parser/renderer tests for complete, incomplete, inconclusive, repeated, empty-category, malformed, unknown-version, and CRLF feedback regions.
- [ ] 1.4 Preserve conservative read compatibility for explicitly typed legacy headings/tags and reject mastery inference from narrative-only legacy results.

## 2. Learning Artifact Scaffold

- [ ] 2.1 Expand the packaged `learning.md` template with explicit excluded-learning, starting-point, stuck-attempt/observation/diagnosis/hypothesis, requested/used hint, remaining-uncertainty, reimplementation, and review prompts.
- [ ] 2.2 Add explicit no-stuck/not-applicable guidance without allowing template placeholders to satisfy required before/after evidence.
- [ ] 2.3 Reserve and document the bounded machine-managed feedback region under `AI 验证记录` while keeping every learner-owned section outside it.
- [ ] 2.4 Update schema/template tests for artifact ordering, tracked task checkboxes, exact headings/markers, Windows paths, and byte preservation of legacy artifacts.

## 3. Verification Gates and Canonical Output

- [ ] 3.1 Extend verify context extraction to include proposal included scope, excluded scope, constraints, and their concrete source references.
- [ ] 3.2 Add scope/constraint gate evaluation and represent each blocker with its contract reference, observed evidence, consequence, and bounded learner next step.
- [ ] 3.3 Separate overall verification disposition from learning status and enforce that only `learning-status: complete` can emit mastered records.
- [ ] 3.4 Render the canonical version-1 feedback region with repeated typed records and no placeholder values for empty categories.
- [ ] 3.5 Replace only the reserved latest-result region and add byte-level tests proving learner reflections, task checkboxes, application files, and test files remain unchanged.
- [ ] 3.6 Add behavioral template tests for software-pass/learning-incomplete, fail/inconclusive gap/review records, multiple blockers, unavailable checks, and clean pass cases.

## 4. Archive Parsing and Replay

- [ ] 4.1 Make archive-feedback evidence extraction prefer the latest valid canonical region and map typed records without model re-inference.
- [ ] 4.2 Apply mastery eligibility and empty-category semantics consistently in initial plan, confirmed apply, and reconciliation.
- [ ] 4.3 Update learner-record deduplication to use type plus normalized topic while preserving original display text.
- [ ] 4.4 Replace synthetic `Mastered topics` retry fixtures with complete verify-shaped `learning.md` artifacts.
- [ ] 4.5 Add a canonical-archive/interrupted-learner-write test that recovers all typed records from the archived artifact alone.
- [ ] 4.6 Add repeated retry, partial legacy evidence, narrative-only legacy evidence, malformed marker, and no-duplicate regression tests.

## 5. Generated Surfaces and End-to-End Verification

- [ ] 5.1 Update canonical verify/archive workflow instructions to describe the versioned feedback contract and per-blocker evidence without exposing parser internals.
- [ ] 5.2 Regenerate every explicitly registered HumanSpec verify/archive skill and command surface and update parity fixtures.
- [ ] 5.3 Extend packed runtime tests to create canonical feedback, interrupt after archive, reconcile through the public CLI, and inspect final learner records.
- [ ] 5.4 Add Windows and CRLF packed-fixture coverage using platform path helpers and exact registered document paths.
- [ ] 5.5 Run targeted tests, full `pnpm test`, `pnpm lint`, `pnpm build`, and package checks before the adaptive-roadmap change builds on this grammar.
