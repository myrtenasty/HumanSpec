## 1. Stack and Shared Sizing Contract

- [x] 1.1 Rebase on the runtime, evidence, and adaptive-roadmap changes so final workflow text references their canonical commands, grammar, and states.
- [x] 1.2 Define one shared sizing structure with stable criterion IDs and rendered guidance for outcome, goals/concepts, tasks, evidence, budget, platforms, unfamiliar concepts, whole-system scope, learner experience, cognitive load, and roadmap impact.
- [x] 1.3 Add unit tests for the shared renderer and explicit criterion completeness without introducing a numeric or file-count score.
- [x] 1.4 Add one shared fit/oversized reporting shape that both propose and next can embed in their learner-facing output contracts.

## 2. Init Prerequisite and Responsibility Corrections

- [x] 2.1 Remove every instruction that runs `openspec init` from inside `humanspec-init` and replace it with read-only prerequisite inspection.
- [x] 2.2 Validate effective HumanSpec profile, explicitly registered workflow surfaces, absence of a public apply workflow, and project-context readiness before document planning.
- [x] 2.3 Add actionable external repair output for missing/inconsistent bootstrap and prove the workflow stops before config, surface, or project-document writes.
- [x] 2.4 Replace historical “not implemented yet” language with stable responsibility/handoff descriptions for init, next, propose, coach, verify, and archive.
- [x] 2.5 Add first-init, partial-doc, valid-repeat, unmarked-file, rejected-update, and no-repeat-bootstrap behavioral tests.

## 3. Propose and Next Sizing Parity

- [x] 3.1 Replace propose's local sizing prose with the shared contract and include every required cognitive/platform/evidence criterion.
- [x] 3.2 Add roadmap-external request handling that reports preserve/interrupt/replace/extend learning-path impact before confirmation.
- [x] 3.3 Replace next's generic “fitting” candidate rule with the same shared contract and evidence reporting shape.
- [x] 3.4 Route oversized candidates to learner-confirmed refinement/propose splitting without silently selecting or creating them.
- [x] 3.5 Add paired propose/next fixtures proving identical context yields identical fit classification and changed classifications identify changed context.

## 4. Coach and Verify Teaching Guards

- [x] 4.1 Require every substantive coach response to label Level 1, 2, or 3 and remind the learner to record the level actually used.
- [x] 4.2 Keep requested and used hint levels distinct and add refusal behavior when asked to write learner evidence directly.
- [x] 4.3 Add verify output categories for blockers, non-blocking suggestions, and follow-up learning while reusing the canonical typed feedback region.
- [x] 4.4 Add the explicit no-complete-solution rule for failing/inconclusive verification and bounded-example guidance.
- [x] 4.5 Add coach escalation/no-write and verify follow-up/complete-patch-refusal behavioral template tests.

## 5. Projection Regeneration and Validation

- [x] 5.1 Update canonical HumanSpec shared/workflow templates only, then regenerate every explicitly registered skill and command projection through the current pipeline.
- [x] 5.2 Update explicit parity hashes/content fixtures and assert no generated surface retains stale bootstrap/status/sizing wording.
- [x] 5.3 Run profile install/update/cleanup tests to ensure regenerated HumanSpec surfaces remain managed without touching user files.
- [x] 5.4 Add cross-platform generated-path tests using `path.join()` expectations for all affected tool adapters.
- [x] 5.5 Run the deterministic HumanSpec capstone subset plus full `pnpm test`, `pnpm lint`, `pnpm build`, and package checks.
- [x] 5.6 Document any unified-manifest migration follow-up without implementing that architectural refactor in this change.
